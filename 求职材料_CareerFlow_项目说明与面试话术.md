# CareerFlow AI · 项目说明书（自用）

> **这份文件是干什么的**
> 你在面试和工作中只讲得清「你真正做过的事」，才立得住。本文件把 CareerFlow AI 项目拆成三块：
> ① **我到底做了什么**（划清边界，不吹也不埋）；② **面试前必须记住什么**（背得下来、答得出来）；③ **怎么跟 HR 说**（大白话版）。
> 末尾附 **第四部分：除现有职责外还需要补的能力**——这部分是你的短板清单，也是你未来 3 周的行动表。
>
> **事实核对基准**：本地 HEAD `08bd639`；线上 https://careerflow-hbem.onrender.com（2026-09-12 实测：`/health` 200、`/api/auth/register/request-code` 200、OpenAPI 已含 `force` 字段）。
> 文中所有数字均来自真实代码与实测，**可以放心用于简历**。

---

## 第 0 部分 · 先背这一句

> **「我做的是一个校园求职场景的全栈 AI 应用：上传简历和岗位 JD，系统用大模型给出可解释的匹配度诊断，还能基于这次诊断直接开启多轮模拟面试。我负责需求定义、方案设计、交互调优和部署上线。」**

这一句是你在任何场合的"锚"。剩下的都是展开。

---

# 第一部分 · 我负责什么、参与什么

## 1.1 角色定位（先认清自己）

你的真实角色是 **「产品负责人 + 交付负责人」**，不是"从零手写代码的工程师"。这个定位在中小厂**同样值钱**，因为中小厂最缺的不是写代码的人，是**能把一个东西从想法推到能用、并推到线上的人**。

| 维度 | 你的角色 | AI 的角色 |
|------|----------|-----------|
| 需求与功能取舍 | ✅ 你定 | 提供选项 |
| 方案设计与技术路线 | ✅ 你拍板 | 出方案、讲清利弊 |
| 代码实现 | ❌ 不写 | ✅ 全部由 AI 生成 |
| 框架/技术选型 | 🔶 你确认 | ✅ 提建议并说明理由 |
| 功能调优与验收 | ✅ 你测、你挑毛病 | 按你的反馈改 |
| 部署上线与线上验证 | ✅ 你操作、你验证 | 给步骤与命令 |
| 文档与交付 | ✅ 你组织 | 起草 |

> **一句话总结**：**AI 是执行者，你是决策者和验收者。** 这句在面试里可以直说，不丢人——2026 年的开发早就这样了，丢人的是"说不清自己做了什么"。

---

## 1.2 我主导的部分（可以理直气壮说"这是我做的"）

| # | 事情 | 具体做了什么 | 可拿出的证据 |
|---|------|--------------|--------------|
| 1 | **需求定义与功能取舍** | 定下"求职管理 + AI 匹配分析 + 模拟面试"三合一闭环；砍掉不做的（不做爬虫抓岗位、不做简历模板生成） | `PROJECT_SUMMARY.md` 功能清单 |
| 2 | **方案设计（核心决策）** | ① 发现"AI 给的匹配分没有依据"，推动评分改为**薪资区间重叠度数据驱动**；② 设计**分析 → 面试一键闭环**；③ 分析结果**去重 + 强制重算**；④ 邮箱验证码选**零成本可插拔**路线 | 提交 `976ea45`、`cf43920`、`e05ef3a` |
| 3 | **交互与逻辑调优** | 站在用户视角走查：发现同一份简历反复分析会堆一堆重复记录、分析完还要手动去面试页重选，推动两次改造 | 同上 |
| 4 | **演示数据设计** | 定下 4 份简历 / 4 份 JD / 5 条分析（含 1 条**故意错配**）/ 5 条覆盖全状态的投递 / 1 场三轮已完成面试——保证**不配 AI key 也能完整演示** | `backend/app/seed.py` |
| 5 | **部署上线与线上验证** | 线上地址 https://careerflow-hbem.onrender.com 已可访问；实测健康检查、接口、注册流程全部正常 | 浏览器可点开 |
| 6 | **文档与交付** | 组织出 `README.md`（含部署步骤）、`PROJECT_SUMMARY.md`（完整项目总结） | 仓库根目录 |

---

## 1.3 我参与执行的部分（实话实说，但说清你的动作）

| 环节 | 我的动作 | 怎么表述 |
|------|----------|----------|
| 代码实现 | 由 AI 生成，**我逐文件读懂、跑起来验证、发现数据不对时反馈修正** | "代码由 AI 辅助实现，我负责跑通与验收，过程中定位并修掉了示例数据不生效、字段不返回等问题" |
| 测试 | 我要求补测试；AI 写用例，**我跑 pytest 确认 29 条全绿** | "建立了 pytest 自动化测试，29 条用例全通过，含越权访问、无 key 降级等边界" |
| 迁移 | AI 写 Alembic 迁移，**我在干净库上跑 `alembic upgrade head` 验证** | "用 Alembic 管理表结构演进，6 个迁移脚本，本地与线上均验证通过" |
| 提交推送 | 我操作 GitHub Desktop 推送 | "用 Git 做版本管理，功能按提交拆分" |
| 构建验证 | 我在改前端构建后跑 `npm run build` 确认类型检查通过 | "前端构建链路改造后，我验证了类型检查与打包产物" |

---

## 1.4 诚实边界（这几句千万别说）

| ❌ 不能说 | ✅ 改成 |
|-----------|---------|
| "这个项目是我写的代码" | "这个项目由我主导设计，代码借助 AI 实现，我负责方案、验收和上线" |
| "我精通 Java/Python/Vue" | "我能读懂项目代码、能改简单逻辑、能跑测试验证，复杂功能借助 AI 完成" |
| "线上有几千用户" | "这是个人作品集项目，非商用，但按真实产品的标准做了鉴权、多租户隔离、部署和降级处理" |
| "我一个人做的" | "需求、设计、调优、部署由我负责，编码环节我借助 AI 工具完成" |

> **重要心态**：中小厂 HR 普遍清楚二本计科学生的真实水平。**你越主动说清边界，越显得可信**；一旦被戳穿"这个你其实不会吧"，整份简历都废。
> 你要传递的信号不是"我代码很牛"，而是——**"给我一个东西，我能把它落地、能跑起来、能上线、出问题我能定位"。**

---

# 第二部分 · 面试前必须记住的关键内容

## 2.1 项目一句话 + 30 秒介绍

**一句话**：面向校招求职者的「简历/JD 管理 + AI 匹配诊断 + 多轮模拟面试」一体化全栈 Web 应用。

**30 秒版**（背下来，面试开场用）：

> "这个项目解决的是校招生最耗神的三件事：海投、判断匹不匹配、准备面试。
> 用户把简历和岗位 JD 录进去，系统调用大模型给出一个**可解释的匹配度诊断**——不是给个黑盒分数，而是拆成**技能、经验、学历、薪资**四个维度分别打分，并说明每一项为什么这么打。
> 然后点一个按钮，就能基于这次诊断**直接进入多轮模拟面试**，AI 逐轮提问、逐轮评分、累计均分。
> 技术上后端 Python FastAPI + 异步 SQLAlchemy，前端 Vue3 + TypeScript，用 Docker 部署在 Render 上，已经可以线上访问。"

---

## 2.2 系统架构与数据流（要能画出来）

```
浏览器
  │  HTTPS
  ▼
Vue3 + Vite 前端（TypeScript / Pinia 状态 / Router 鉴权守卫）
  │  /api/*  +  JWT Token
  ▼
FastAPI 后端（异步）
  │  ├── 鉴权依赖 get_current_user —— 每个接口按 owner_id 过滤数据
  │  ├── 业务逻辑（简历 / JD / 分析 / 投递 / 面试）
  │  └── 外部调用 ask_llm()
  ▼                          ▼
数据库                 大模型（OpenAI 兼容接口）
SQLite(本地/线上)      智谱 glm-4-flash / 通义 qwen-plus
PostgreSQL(生产可选)
```

**三个必须记住的点**：

1. **同源托管**：线上前端不是单独部署的，Docker 构建时把前端打包产物放进后端的 `static` 目录，由后端一起提供。**好处：没有跨域问题**（面试官很吃这一套，能说出"为什么这么设计"就是加分项）。
2. **多租户隔离**：每张业务表都有 `owner_id`，所有接口都按当前登录用户过滤。**越权访问返回 404 而不是 403**——故意这么设计，避免泄露"这个资源到底存不存在"。
3. **外部依赖降级**：AI 调用失败（没配 key / 超时 / 限流 / 返回非法格式）时，接口返回 **503 + 明确提示**，而不是整个服务崩掉。**页面其他功能照常可用**。

---

## 2.3 技术选型理由（HR/面试官会问"为什么用这个"）

| 选了 | 为什么 | 一句话答法 |
|------|--------|-----------|
| **FastAPI**（Python） | 原生异步、自动生成接口文档（Swagger）、生态成熟 | "AI 类应用调用外部模型耗时长，异步框架能避免请求互相阻塞；而且自动生成接口文档，联调省事。" |
| **SQLAlchemy 2.0 异步** | 模型可跨数据库移植，本地 SQLite / 生产 PostgreSQL 同一套代码 | "开发用 SQLite 零依赖起步，需要时切 PostgreSQL 不用改代码。" |
| **Alembic 迁移** | 表结构演进可追溯、可回放 | "6 个迁移脚本管理从建表到薪资字段、验证码表、唯一索引的全部变更。" |
| **Vue3 + TypeScript** | 组件化、类型检查能提前发现错误 | "前端构建时会先跑类型检查，类型不对直接构建失败。" |
| **Element Plus** | 企业后台风格组件，开发快 | "并且做了**按需引入**优化，没用的组件不打进包里。" |
| **JWT** | 无状态鉴权，前后端分离友好 | "密码只存哈希，登录后发 Token，前端自动携带。" |
| **Docker + Render** | 一条命令跑全栈，云上部署简单 | "多阶段构建把前后端打成一个镜像，同源托管。" |

---

## 2.4 三个能讲深的设计故事（这是你面试的"硬通货"）

> 面试官不关心你会不会背 API，只关心**你遇到问题时怎么想**。下面三个故事是你最值钱的部分，每个都要能讲 2 分钟。

### 故事一：把"AI 拍脑袋的分数"改成"有据可查的分数"⭐ 最重要

**问题**：早期版本里，AI 给的匹配度分数里有个"薪资匹配 82 分"，但简历里**根本没有薪资信息**。用户（我）一看就知道这分数是瞎编的。

**分析**：根因是——**输入信息缺失，模型只能编**。这是所有 LLM 应用的通病：**你给不出依据，它就给你幻觉。**

**方案**：
1. 简历表新增 `expected_salary`（期望薪资，如 `9k-13k`），JD 表新增 `salary_range`（岗位薪资范围，如 `8k-14k`）。
2. 改 Prompt：把这两个字段**原文喂给模型**，并给出硬规则——**按区间重叠度打分**：高度重叠给 ≥85，部分重叠 60–84，期望明显超出上限或低于下限给 ≤45；**没有信息就不许瞎估**。
3. 强制模型在诊断文字里**逐条点名**四个维度的打分依据。
4. 解析层做防御：分数钳制在 0–100、去掉"85分"里的单位、非法格式整条返回 503 而不是 500。

**效果**：示例数据变得自洽，而且**能被解释**——"实施顾问简历（期望 9k-13k）投实施岗（8k-14k）→ 薪资契合 80 分"；"同一份简历投后端岗（13k-20k）→ 只在 13k 窄幅重叠 → 薪资契合 60 分，总分 52，判定错配"。**低分也能说得清为什么低。**

**这段怎么讲**：
> "我发现 AI 给出的薪资评分是没有依据的，因为输入里压根没有薪资字段。所以我做了两件事：一是把薪资信息补进数据模型，二是把评分规则从'让模型自由发挥'改成'按区间重叠度算'。改完之后分数变得可解释了，用户能拿着这个分数去跟面试官解释为什么我期望这个数。"

---

### 故事二：同一份简历反复分析，数据库堆了一堆重复记录

**问题**：用户每点一次"开始分析"，系统就**无条件调一次大模型**并**新建一条记录**。同一份"简历 × 岗位"点 5 次，就有 5 条几乎一样的记录，而且**每次都真实消耗 AI 调用成本和时间**。

**方案（四层设计）**：
1. **去重键**：同一用户的同一「简历 × 岗位」只允许一条结果。
2. **命中缓存**：进来先查，已有结果且没要求重算 → **直接返回旧的，不调 AI、不写库**。
3. **强制重算开关**：加一个 `force` 参数（前端是"重新分析"按钮）。**这一条很关键**——不加的话，用户改了简历再点分析，分数不变，反而显得系统是坏的。
4. **数据库兜底**：加唯一索引 `(owner_id, resume_id, job_id)`，从数据库层防止并发产生的重复。

**这里有个细节，讲出来很加分**：
> "去重键里我特意加了 `owner_id`。因为不同用户的数据自增 ID 可能撞车，如果只用『简历 ID + 岗位 ID』做唯一约束，A 用户的分析会把 B 用户的挡掉。加上 `owner_id` 之后，每个用户的数据互不干扰。"

**面试官听到"我想到了跨用户 ID 冲突"这句，会直接把你从"会写 CRUD 的学生"划到"有工程意识的人"。**

---

### 故事三：邮箱验证码——怎么做到"零成本还不影响演示"

**问题**：注册想加邮箱验证码显得正规。但真实发邮件需要配置 SMTP 服务（有成本、要域名、要维护），而这是个**个人作品集项目，不商用、不对外开放**。

**方案：可插拔设计**
- 系统检测三个环境变量（`SMTP_HOST` / `SMTP_USER` / `SMTP_PASSWORD`）**是否都配齐**：
  - **配齐了** → 真发邮件（兼容 Resend / Gmail / 阿里云邮件推送等标准 SMTP）。
  - **没配齐** → 进入**开发态**：不真发信，验证码由接口直接返回并显示在前端页面上。
- 完整流程照做：**两步注册**（先请求验证码 → 再校验码建号）、**60 秒冷却**防刷、**验证码表**持久化、**5 条测试用例**覆盖。

**这段怎么讲**：
> "我想清楚了这个项目的定位——个人作品集，不商用。花成本接真实的邮件服务不划算，但流程和数据结构要按真实标准做。所以我做成了可插拔的：现在演示态下验证码直接显示，将来配好 SMTP 变量就自动切换成真实发信，业务代码一行都不用改。"

**这个故事的真正价值**：它证明你会**权衡**——**知道什么场景该花成本，什么场景不该**。这是实施顾问和产品岗最看重的能力。

---

## 2.5 调优依据与可量化成果（简历里的数字就靠这些）

### 成果一：前端打包体积优化（按需引入）

**问题**：前端把整个 Element Plus 组件库（几十个组件 + 全部样式）全量打包进主包，**哪怕只用到了表格、弹窗、按钮几个组件**。

**做法**：引入两个构建期插件，自动分析模板里真正用到的组件，**只打包用到的**，样式也按需注入；同时移除代码里手写的全局引入。

**结果（同份源码，构建产物对比）**：

| 指标 | 改动前 | 改动后 | 降幅 |
|------|--------|--------|------|
| 主 JS 包 | **1.11 MB** | **310 KB** | **↓ 72%** |
| 主 CSS | **364 KB** | **42 KB** | **↓ 88%** |
| 前端产物总体积 | 1.5 MB | 958 KB | ↓ 37% |

**额外价值**：优化后组件变成独立的小文件，**用户用到才下载**，首屏只加载必要代码。

> **面试怎么讲**："首屏要加载的主包从 1.11MB 降到 310KB，降了七成，用户打开页面明显更快。做法是改成按需打包，没用到的组件不打进包里。"

### 成果二：AI 分析去重（省钱省时间）

- 同一「简历 × 岗位」重复点击分析 → **不再重复调用 AI**，直接返回已有结果。
- 效果：**减少 AI 调用次数**、分析列表不再堆积重复记录、响应更快。
- 保留"重新分析"按钮，需要刷新时手动触发。

### 成果三：工程质量

| 指标 | 数值 |
|------|------|
| 自动化测试 | **29 条用例全部通过**（含鉴权、越权隔离、无 key 降级、去重逻辑） |
| 代码规范检查 | **零告警** |
| 数据库迁移脚本 | **6 个**（从建表到薪资字段、验证码表、唯一索引） |
| 后端代码量 | 19 个模块 / 约 2356 行 |
| 前端代码量 | 8 个页面 + 组件 / 约 2488 行 |

### 成果四：开箱即用的演示数据

**即使不配置任何 AI 密钥，打开网站也能看到完整内容**：4 份简历、4 份 JD、5 条带四维评分的分析（含 1 条刻意设计的低分错配）、5 条覆盖全部状态的投递记录、1 场已完成的三轮模拟面试（得分 82→80→84，均分 82）。

> **为什么这件事值得讲**：说明你想到了"别人第一次打开会看到什么"。**作品集如果打开是空白，等于没做。**

---

## 2.6 部署流程要点（你要能完整复述）

### 部署架构

**Docker 多阶段构建 → Render 云平台（免费实例）→ 前后端同源托管**

**第一阶段**：用 Node 环境把 Vue 前端构建成静态文件。
**第二阶段**：用 Python 环境装后端依赖，**把第一阶段的产物拷进后端的静态目录**。
**结果**：一个镜像同时提供网页和接口，**只有一个服务、一个域名、没有跨域**。

### 部署五步（背这五步就够）

| 步骤 | 关键点 |
|------|--------|
| 1. 代码推到 GitHub | Render 从 GitHub 拉代码自动构建 |
| 2. 创建 Web Service | Runtime 选 **Docker**，Branch 选 `main`，Plan 选 **Free** |
| 3. 配置环境变量 | 见下表——**密钥只配在平台控制台，绝不写进代码仓库** |
| 4. 首次构建 | 约 4–8 分钟 |
| 5. 线上验证 | 打开根路径应是**网页界面**（不是接口文档）；用演示账号登录；各页面有示例数据 |

### 必须配置的环境变量

| 变量 | 作用 | 注意 |
|------|------|------|
| `SECRET_KEY` | 登录令牌的签名密钥 | 用随机长串，**不能泄露** |
| `LLM_API_KEY` | 大模型密钥 | 只在控制台填，**不进仓库** |
| `LLM_BASE_URL` / `LLM_MODEL` | 模型服务地址与名称 | 可切换不同厂商 |
| `AUTO_SEED` | 自动灌入演示数据 | 免费实例磁盘是临时的，**重启会清库**，靠它兜底 |
| `SMTP_*` | 邮件服务 | 不配 = 开发态，验证码前端直接显示 |

### 三个"坑点"（面试问"部署遇到什么问题"就答这些）

1. **免费实例会休眠**：15 分钟没流量就休眠，**下一次访问冷启动要等 30–60 秒**。所以要提前预热再演示。
2. **磁盘是临时的**：重启后数据会清空。做**自动灌演示数据**的兜底逻辑，保证重启后打开仍有内容。
3. **密钥绝不能进仓库**：本地用 `.env` 文件（已被 Git 忽略），线上在平台控制台配置。**这是安全红线**。

### 线上验证清单（部署后必做，也是你面试能说的"我做了线上验证"）

- [x] 健康检查接口返回 200
- [x] 根路径显示网页界面（不是接口文档）
- [x] 用演示账号能登录
- [x] 简历 / JD / 分析 / 投递 / 面试 各页面有数据
- [x] 注册流程能走通（两步验证码）
- [x] 重复点击分析会命中缓存（提示"未重复调用 AI"）

---

## 2.7 项目亮点清单（背这 5 条，任何场合都能用）

1. **完整的业务闭环**：录入简历/JD → AI 多维诊断 → 一键进入模拟面试 → 投递进度跟踪，**不是零散的功能堆砌**。
2. **可解释的 AI**：分数拆成四维并说明依据，薪资维度用**区间重叠度**硬规则计算，**拒绝黑盒**。
3. **真实的工程考量**：多租户数据隔离、越权返回 404、外部依赖失败降级、数据库迁移、老库平滑升级。
4. **有据可查的性能优化**：主包体积降低 72%、样式降低 88%，**有前后对比数据**。
5. **开箱即用的演示**：不配任何密钥也能完整演示，**降低了别人体验的门槛**。

---

## 2.8 已知限制与诚实应答（被问到就这么答）

| 可能被问 | 诚实且加分的答法 |
|----------|------------------|
| "有真实用户吗？" | "没有，这是个人作品集项目。但我按真实产品标准做了鉴权、数据隔离和部署，不是玩具 Demo。" |
| "测试覆盖多少？" | "29 条接口测试全部通过，覆盖注册登录、数据隔离、越权访问、AI 调用失败的降级、分析去重。**没做前端自动化测试**，前端我是手工走查的。" |
| "为什么数据库用 SQLite？" | "免费实例下 SQLite 最省事、零维护成本。代码层用 ORM 做了跨库可移植，需要时可以切 PostgreSQL，迁移脚本在两种库上都验证过。" |
| "代码是你自己写的吗？" | "**核心方案和设计是我定的，编码环节借助 AI 完成。**我能读懂项目代码、能改简单逻辑、能跑测试验证结果。我扮演的是把需求落地成能用产品的人。" |
| "你觉得这个项目最大的问题是什么？" | "**没有真实用户反馈**，很多设计是我自己推演的。另外前端自动化测试是空白，这部分是我下一步要补的。" |

---

# 第三部分 · 怎么跟 HR 说

## 3.1 HR 版介绍（不要讲技术名词）

HR 听不懂"异步 SQLAlchemy""按需引入"，她们只判断三件事：**你做过什么、能不能干活、好不好沟通。**

### 30 秒版（自我介绍里用）

> "我做过一个求职类的网站项目，叫 CareerFlow。它帮找工作的学生管理简历和岗位，用 AI 分析简历和岗位的匹配度，还能模拟面试练习。
> 这个项目从功能设计、界面优化到部署上线都是我负责推进的，现在可以在线访问。"

### 1 分钟版（HR 追问"讲讲你的项目"）

> "这个项目的出发点是——我自己找工作的时候，最大的困扰是投了好多岗位但不知道哪个值得投，面试也没底。
> 所以我做了个工具：把简历和岗位信息录进去，系统会给出一个**匹配度评分**，而且不是简单给个分数，会分**技能、经验、学历、薪资**四个方面分别说明理由，让你知道差在哪。
> 然后可以点一个按钮，**直接进入模拟面试**，AI 会像面试官一样一轮一轮提问、打分。
> 整个项目我负责的是**要做什么功能、怎么做更好用、以及最后部署到线上**。现在打开网址就能用，里面预置了示例数据，不用注册也能看。"

### 追问"你具体做了哪些事"（HR 最常问的一句）

> "我主要做三块：
> **第一，功能设计**。定下来要做哪些功能、哪些不做。比如我发现 AI 给的薪资评分是没依据的，就推动把薪资信息补进去，让评分变得讲得通。
> **第二，优化体验**。比如同一个简历反复分析会产生很多重复记录，我改成了有记录就直接用、需要更新时手动刷新，既省时间也省钱。
> **第三，部署上线**。把项目部署到云服务器上，配置好各项参数，并且上线后一条条验证功能是否正常。
> **代码实现部分我是借助 AI 工具完成的**，我能读懂、能改，但复杂逻辑不是我一行行敲的。"

> ⚠️ **最后那句一定要主动说**。主动坦白 = 诚实可靠；被问出来 = 减分。而且中小厂 HR 对二本学生的代码水平本来就有预期，**你的加分项是"流程清楚、能落地、沟通直白"**。

---

## 3.2 简历里的项目经历写法（可直接用）

**CareerFlow AI · 智能求职助手**　个人项目 ｜ 2026.06 – 2026.09

> 面向校招场景的全栈 AI 应用，实现"简历/JD 管理 → AI 匹配诊断 → 模拟面试 → 投递跟踪"完整闭环。技术栈：Python FastAPI + 异步 ORM + Vue3 + TypeScript + Docker + Render。

- **主导方案设计与功能迭代**：发现 AI 评分缺乏依据，推动简历/岗位补充薪资字段，将薪资维度改为按**区间重叠度**计算的硬规则，并要求模型逐维度输出打分理由，使分数从"黑盒"变为**可解释、可向面试官说明**。
- **优化核心交互逻辑**：针对同一"简历×岗位"重复分析产生冗余记录、且每次重复消耗 AI 调用的问题，设计**缓存去重 + 强制重算**机制（唯一索引按用户隔离，避免跨用户 ID 冲突），减少无效调用并保持数据整洁。
- **主导部署上线与线上验证**：采用 **Docker 多阶段构建**将前端产物与后端服务打包为单镜像同源托管（规避跨域）；配置环境变量、健康检查与数据自动初始化机制，应对云平台**临时文件系统重启丢数据**问题；上线后逐项验证接口与业务流程。
- **推进工程质量建设**：建立 **29 条接口自动化测试**（覆盖鉴权、数据隔离、越权访问、外部依赖降级、去重逻辑）全部通过，代码规范检查零告警；完成 **6 个数据库迁移脚本**，支持旧库平滑升级。
- **性能优化**：将前端组件库改为**按需构建**，首屏主包体积由 1.11MB 降至 310KB（降低 72%），样式文件由 364KB 降至 42KB（降低 88%）。

> **写法说明**：每条都用「**动词 + 做了什么 + 达成什么结果**」的结构，且**都指向"我判断、我推动、我验证"**，而不是"我实现了 XX 功能"。这正是你想要的——**HR 看到的是"能独立干活的人"，而不是"会写代码的人"。**

---

## 3.3 高频问题应答卡

| 问题 | 你的答法（口语版） |
|------|-------------------|
| **这项目你做了多久？** | "断断续续三个月，主要是下班和课余时间做。前面在做需求和设计，中间开发，最后部署和调优。" |
| **遇到最大的困难是什么？** | "最大的困难不是写代码，是**发现设计有问题**。一开始 AI 给的评分看着挺像样，但我一问'这分数凭什么'就答不上来。后来我把评分改成有明确计算规则的，才踏实。这件事让我意识到，**AI 会给看起来很对但没依据的结果**，必须自己验证。" |
| **怎么保证 AI 生成的代码是对的？** | "三个办法：一是**跑测试**，29 条用例全过；二是**实际点一遍**，页面上每个功能我都手测；三是**看数据**，直接查数据库确认字段有没有真的存进去。" |
| **如果上线后崩了怎么办？** | "先看平台日志定位是启动问题还是运行时报错。因为代码在 GitHub 上，可以回滚到上一个版本重新部署。另外数据库是临时的，重启会重建，所以业务数据不会彻底丢。" |
| **为什么不做成商用？** | "定位就是作品集，投入产出比不合适。但我按真实标准做了鉴权、数据隔离和降级处理——**该有的工程规范都有，只是没有商业化的部分**。" |
| **你觉得你适合什么岗位？** | "**实施工程师/数字化实施顾问**。因为我擅长的是搞清楚业务要什么、把方案落地、上线之后保障能用，而不是纯写代码。这个项目正好把这条链路走了一遍。" |

---

## 3.4 话术禁忌

| ❌ 别这么说 | 为什么 |
|------------|--------|
| "我用 AI 写的，我不太会" | 太消极。改成"代码借助 AI 实现，方案和落地由我负责，我能读懂和修改" |
| "这个功能很简单" | 主动贬低自己的产出。改成"这个功能我做了 XX 考虑" |
| 一开口就讲技术栈名词 | HR 听不懂。先讲**解决什么问题**，技术细节留给技术面 |
| "我都会""精通" | 一问就穿帮。用"用过""了解""能上手" |
| 只讲功能不讲取舍 | 讲你**砍掉了什么、为什么砍**，比讲做了什么更显成熟 |

---

# 第四部分 · 除现有职责外，我还需要补的能力

> **为什么单列这一部分**：你现在做的是"我提需求 → AI 实现 → 我验收"。这个模式能做出作品集，**但撑不住日常工作**——因为工作中更常见的场景是"**别人给你一个已有的项目，让你改一处、加一个功能、查一个问题**"。那时候没人帮你从零开始设计，你必须**能进到代码里**。

## 4.1 目标画像

**"能独立干活" = 下面这 6 件事你能自己完成：**

1. 把项目在本地跑起来（装依赖、起服务、开网页）。
2. 看懂报错信息，知道去哪找原因。
3. 找到某个功能对应哪个文件、哪段代码。
4. 做小改动：改文案、改字段、改校验规则、加一个简单接口。
5. 改完能验证：跑测试、点页面、查数据库。
6. 把改动提交到 Git 并推送。

**这 6 件事全部不需要你"精通编程"，只需要熟练。这是你 3 周能达到的水平。**

---

## 4.2 必需补的 6 项（按优先级排序）

| 优先级 | 能力 | 为什么必须 | 具体到什么程度算够 |
|--------|------|-----------|-------------------|
| **P0** | **能本地跑起项目 + 看日志** | 不会跑起来，一切免谈。日常工作中"帮我看看这个问题"第一步就是这个 | 能独立启动前后端、打开接口文档、在控制台看到错误日志 |
| **P0** | **Git 基本操作** | 团队协作的入场券，每天都在用 | 会 `add / commit / push / pull`，能看懂分支，遇到冲突知道怎么处理 |
| **P0** | **HTTP 接口调试** | 前后端联调、验证接口全靠它 | 会用 Swagger 文档发请求、会看浏览器 F12 的 Network、能看懂状态码（200/401/404/500） |
| **P1** | **SQL 基础查询** | 验证数据、排查问题最直接的手段 | 会 `SELECT ... WHERE ... JOIN`，能自己写查询确认数据存对没存对 |
| **P1** | **看懂代码结构** | 从"这是什么"到"在哪改"的能力 | 拿到一个陌生项目，能说出哪个文件负责什么、一个请求从哪进从哪出 |
| **P1** | **修改简单逻辑** | 从"提需求"升级到"能动手" | 能改校验规则、加个字段、调个接口参数，能看懂自己在改什么 |

---

## 4.3 三周补课清单（每周约 4 小时，用你现在的项目练）

> **核心方法：不要另找教程项目，就用 CareerFlow 练。** 你已经熟悉它，学起来没有额外认知负担。

### 第 1 周：跑起来 + 会调试

| 任务 | 练什么 | 验收标准 |
|------|--------|----------|
| 本地启动后端，访问接口文档页面 | 环境准备、命令行基础 | 浏览器能打开 `localhost:8000/docs` |
| 本地启动前端，登录演示账号 | 前后端联调 | 页面上能看到示例数据 |
| 在接口文档里手动调用"登录"和"查简历"接口 | 接口调试 | 能拿到 Token 并用它调通需要登录的接口 |
| 故意输错密码，看返回什么 | 状态码理解 | 能说出 401 是什么意思、为什么不是 500 |
| 把服务停掉再访问，看报什么错 | 错误定位 | 能分辨"服务没起来"和"代码报错"的区别 |

### 第 2 周：看代码 + 做小改动

| 任务 | 练什么 | 验收标准 |
|------|--------|----------|
| 顺着一个请求走一遍：从点按钮到数据存库 | 代码结构理解 | 能用嘴说出这条链路经过哪些文件 |
| 把某个页面的按钮文案改一个字，重新构建看效果 | 改动闭环 | 改完能在页面上看到变化 |
| 加一条校验规则（比如简历标题不能为空） | 业务逻辑修改 | 前端给出提示、后端也拦截 |
| 用数据库工具打开 SQLite 文件，看看简历表长什么样 | SQL 基础 | 能查出刚才新建的那条记录 |
| 跑一次全部测试，故意改坏一处再看测试报错 | 用测试保护自己 | 能看懂测试失败信息指出哪一行 |

### 第 3 周：部署 + 独立闭环

| 任务 | 练什么 | 验收标准 |
|------|--------|----------|
| 自己完整走一遍部署流程（可先用测试分支） | 部署能力 | 能说出五个步骤和每个环境变量的作用 |
| 在平台上看一次运行日志 | 线上排障 | 能找到服务启动日志和错误日志 |
| 做一次"改代码 → 提交 → 推送 → 自动重新部署 → 线上验证"完整循环 | 独立闭环 | 线上能看到自己的改动 |
| 准备一段 3 分钟的演示（现场打开网站走一遍） | 表达能力 | 不卡壳、能应对"这里为什么这么设计" |

---

## 4.4 面试前自检清单

**技术侧**
- [ ] 能在 5 分钟内把项目在本地跑起来
- [ ] 能说出请求从浏览器到数据库经过哪几个环节
- [ ] 能讲清三个设计故事（薪资评分 / 分析去重 / 验证码方案）
- [ ] 能背上四个数字：**29 条测试**、**主包降低 72%**、**样式降低 88%**、**6 个迁移脚本**
- [ ] 能复述部署五个步骤和三个坑点（休眠、临时磁盘、密钥安全）
- [ ] 被问"代码是不是你写的"时，能坦然说清边界

**表达侧**
- [ ] 30 秒版和 1 分钟版介绍各练过 3 遍
- [ ] 会用"**解决什么问题**"开头，而不是"**用了什么技术**"开头
- [ ] 准备好一个"**我发现了什么问题、怎么解决的**"故事（推荐讲薪资评分那个）
- [ ] 准备好一个"**这个项目还差什么**"的诚实回答

---

## 附：一页速查卡（打印出来贴在桌上）

```
【一句话】校园求职场景的全栈 AI 应用：简历/JD 管理 → AI 匹配诊断 → 模拟面试 → 投递跟踪

【我做什么】需求定义 · 方案设计 · 交互调优 · 部署上线 · 线上验证
【AI 做什么】代码实现（我负责读懂、验收、修正）

【三个故事】
 1. 薪资评分：没依据 → 补字段 + 区间重叠度硬规则 + 强制说明理由
 2. 分析去重：重复调用浪费 → 缓存 + 强制重算开关 + 唯一索引（含 owner_id 防串档）
 3. 验证码：成本权衡 → 可插拔，配了 SMTP 就真发，没配就开发态显示

【四个数字】29 条测试全过 ｜ 主包 1.11MB→310KB(-72%) ｜ CSS 364KB→42KB(-88%) ｜ 6 个迁移

【部署五步】推 GitHub → 建 Docker 服务 → 配环境变量 → 等构建 → 线上逐项验证
【三个坑】免费实例休眠冷启动慢 ｜ 磁盘临时重启清库(AUTO_SEED 兜底) ｜ 密钥绝不进仓库

【线上地址】https://careerflow-hbem.onrender.com
【演示账号】demo@careerflow.app / CareerFlow2026

【红线】不说"代码是我写的"；主动说明"方案我定、代码 AI 实现、我能读懂能改能验证"
```

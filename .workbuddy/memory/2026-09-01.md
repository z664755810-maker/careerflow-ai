# 2026-09-01 工作记录

## CareerFlow 部署就绪（借鉴 RAG 项目的 Render 剧本，适配而非照搬）

参考 `D:\Workbuddy\project-2\docs\RENDER_DEPLOY_PLAYBOOK.md`（RAG 项目已跑通的 Render 部署流程），
把渲染部署逻辑套用到 CareerFlow，关键**借鉴点**与**必要改动**：

### 直接套用（同构）
- 多阶段 Dockerfile：阶段1 `node:20` 构建前端 dist，阶段2 `python:3.13` 跑后端并把 dist 拷入 `/app/static` 同源托管（免 CORS）。
- `start.py` 直接读 `os.getenv("PORT")` 启动，绕开 shell `$PORT` 展开（Render 通用）。
- 自动 seed 应对 Render 临时文件系统重启丢库。
- `backend/static/` 已加进 `.gitignore`，避免把构建产物提交进仓库。

### 必须改（不能照搬 RAG）
- **Python 版本**：RAG 锁 3.12 是因为 Chroma 无 cp313 wheel；CareerFlow 不用 Chroma，保持 3.13（与本地环境一致）。
- **配置键**：RAG 用 `EMBEDDING_PROVIDER`/`DASHSCOPE_API_KEY`/`CHROMA_PERSIST_DIR`，CareerFlow 无 embedding/Chroma，
  改用 `LLM_API_KEY`/`LLM_BASE_URL`/`LLM_MODEL`；并把「免费档 429」风险落到 LLM 层 → 推荐 dashscope(qwen-plus) 而非智谱免费档。
- **seed 内容**：RAG 灌面试题向量，CareerFlow 灌「演示账号 demo@careerflow.app + 示例简历 + 示例 JD」，让 AI 分析开箱可用。

### 新增/改动文件
- 新增：`Dockerfile`(根, Render 用)、`.dockerignore`、`render.yaml`、`backend/start.py`、`backend/app/seed.py`
- 改：`backend/app/main.py`(lifespan 后台 seed + 同源静态托管 catch-all，仅 `_HAS_FRONTEND` 时启用)、
  `backend/app/config.py`(加 `auto_seed`)、`backend/tests/conftest.py`(设 `AUTO_SEED=0` 防测试被灌)、
  `backend/.env.example`、`frontend/src/views/LoginView.vue`(演示账号提示)、`README.md`(部署段重写为单服务同源托管)、`.gitignore`

### 验证（全部实跑通过）
- pytest 11/11、ruff 全过（未破坏既有逻辑）。
- 模拟 Docker 布局：拷贝 frontend/dist→backend/static，`start.py` 用 `PORT` 环境变量起服务，
  实测 `/` 返回 SPA HTML、`/assets/*.js` 200、SPA 深链 `/resumes` 回退 index.html 200、
  `/health` 返回 JSON、`/api/auth/me` 无 token 401、`/docs` 200；seed 成功灌入演示账号+简历+JD。
- 注意：真实健康端点是 `/health`（非 `/api/health`），render.yaml 的 healthCheckPath 已设为 `/health`。

### 待用户操作（我无 Render 凭证，无法代点控制台）
1. 推送本仓库到 GitHub（用户用 GitHub Desktop 连 Render 更顺）。
2. Render 控制台 New → Web Service → 选仓库 → Runtime=Docker → 设环境变量：
   `SECRET_KEY`(随机)、`LLM_API_KEY`(dashscope)、`LLM_BASE_URL`/`LLM_MODEL`、`AUTO_SEED=1`。
3. 部署后浏览器实测：演示账号登录 → AI 分析页选示例简历+JD → 出匹配度+建议+面试题。

## 扩充示例数据（让每个功能都能演示、结果更直观）

用户要求"新增示例、把示例做更完善、每个功能都能展示、数据多、结果直观"。改动：
- `backend/app/seed.py`：演示账号不变；简历 1→4（实施/后端/前端/数据，内容写实）、JD 1→4（同四方向）；
  **新增预置 5 条 Analysis 历史**（评分 86/81/79/83/52，含一份错配 52 展示低分形态），让「历史分析」一进入就有数据。
  预置分析为「示例分析数据」，用于演示形态；配 LLM_API_KEY 点「开始分析」会生成实时结果追加。
- `frontend/src/views/AnalysisView.vue`：历史表格新增「简历/JD」标题列 + 评分 el-tag 配色（≥70 绿/<70 黄），更直观。
- `LoginView.vue` 演示提示更新；`README.md` 部署/验证段同步说明 4+4+5 数据。
- 验证：ruff 全过、pytest 11/11、前端 build 通过；模拟 Docker 跑真实服务确认登录后简历=4/JD=4/历史分析=5，外键与 questions JSON 均正确。

注意（交付给用户）：线上 Render 旧库已是旧 seed(1+1)，seed 仅在库空时灌入；**push 触发重新部署后，
Free 实例 SQLite 临时盘会重置，新 seed 自动生效**。若不想重部署也能看新数据，需在 Render 控制台清库或等实例冷启。

## AI分析删除功能 + 整体UI升级 + 业务扩展方案

用户反馈：① AI分析界面要加删除；② 整体UI太简单普通需优化；③ 项目太简单难切合实际场景，要扩展方案（非必要）。

### 1. 删除功能
- 后端 `backend/app/api/analysis.py` 新增 `DELETE /api/analysis/{id}`（按 owner_id 隔离，越权返回 404，防泄漏存在性）。
- 前端 `api.ts` 加 `deleteAnalysis()`；`AnalysisView.vue` 历史表加「查看/删除」操作列 + 确认弹窗 + 详情弹窗（仪表盘式展示评分与面试题）。
- 测试：`test_analysis.py` 新增删除+越权隔离 2 例，pytest 升至 13 个全过。

### 2. UI 升级（indigo/violet 主题，脱离朴素表单）
- 新增 `frontend/src/styles/global.css`（主题色、卡片质感、侧边栏样式），`main.ts` 引入。
- `App.vue` 改为**左侧深色侧边栏布局**（品牌 + 导航 + 用户信息 + 退出）。
- 新增 `DashboardView.vue`「概览」仪表盘（简历/JD/历史/平均分 4 张统计卡 + 最近分析表），`/` 重定向到 `/dashboard`。
- `LoginView.vue` 改 **SaaS 双栏**（左品牌 banner + 右登录卡）。
- `ResumeView`/`JobView` 列表加「查看」弹窗预览全文（演示不用进编辑框）；统一页面标题区样式。
- 验证：前端 `npm run build` 通过（1679 模块，TS 编译过）。构建有 1.1MB 主 chunk 警告（Element Plus 全量引入），非阻塞，如需可后续按需引入优化。

### 3. 业务扩展方案
- 新增 `EXTENSIONS.md`：分析"太简单/不贴实际"的根因 + 按真实招聘场景的扩展清单。
  P0：① 简历/JD 文件上传解析(pdfplumber/docx2txt) ② 投递管理看板(Application Tracker 闭环) ③ 多维匹配雷达图；
  P1：模拟面试对话(SSE)、报告PDF导出/JSON备份、搜索筛选标签、多账号协作；
  P2：薪资估算/岗位推荐/定时提醒/暗色模式。附技术债提醒（Free实例重启丢库、LLM无缓存、缺上传校验）。

### 验证与交付
- ruff 全过、pytest 13/13、前端 build 通过；模拟 Docker 跑真实服务确认删除端到端：demo登录→5条→删id=1返回204→越权删id=2返回404→删后剩4条。
- 提交 `81040e6`（工作树干净；`*.tsbuildinfo` 新增进 .gitignore）。**推送仍交用户（本环境推 GitHub 偶发 TLS 中断，GitHub Desktop 通）。**
- 用户核心顾虑"项目太简单不贴实际"→ 用 EXTENSIONS.md 给出可落地的真实业务扩展路线回应，并建议优先做 P0-1/2 拉升作品集含金量。

## P0-1 文件上传解析 + P0-2 投递管理看板（落地实现）

用户"现在开始吧"→ 实现 EXTENSIONS.md 的 P0-1、P0-2，消除"手填纯文本"的玩具感、闭合求职链路。

### P0-1 文件上传解析
- 新增 `backend/app/upload_utils.py`：扩展名白名单 `.txt/.md/.pdf/.docx` + 3MB 上限 + 空内容拒绝（抛 ValueError→路由转 400）。
  PDF 用 `pypdf.PdfReader`，Word 用 `python-docx`，纯文本 utf-8/gbk/latin-1 兜底解码。
- `resumes.py` 加 `POST /resumes/upload`、`jobs.py` 加 `POST /jobs/upload`（multipart `file` + `title` 表单字段），落 `content`/`description`。
- `requirements.txt` 加 `pypdf>=4.0`、`python-docx>=1.1`。
- 前端 `ResumeView`/`JobView` 加隐藏 `<input type=file>` + 「📎 上传文件」按钮 + 成功/失败提示；`api.ts` 加 `uploadResume/uploadJob`。

### P0-2 投递管理看板
- `models.py` 新增 `Application`（`resume_id`/`job_id` 外键 SET NULL + `resume_title`/`job_title` 快照 + `status` + `applied_at` + `notes`）+ 常量 `APPLICATION_STATUSES`。
- `schemas.py` 加 `ApplicationCreate/Update/Out`，状态校验用 pydantic `@field_validator`（非法→422，非 500）。
- `api/applications.py`：`POST/GET/GET{id}/PUT/DELETE`，全按 `owner_id` 隔离（越权 404），`GET` 支持 `?status=` 过滤（非法 400）。
- `seed.py` 加 `SAMPLE_APPLICATIONS` 5 条（覆盖全部状态，含 applied_at 偏移），让看板开箱有数据。
- 前端新增 `ApplicationsView.vue` 看板（5 列状态分栏 + 统计卡 + 卡片内联改状态/编辑/删除）；`DashboardView` 加「投递记录」卡；`App.vue`/`router` 注册 `/applications` 导航。

### 关键修复：seed 门控解耦
- 原 `main.py` 中 seed 仅在 `_HAS_FRONTEND`(有 static 目录) 时运行；但 Render Docker 镜像只装后端、无 static 目录 → 线上永远不灌数据。
  改为仅由 `AUTO_SEED` 控制（与前端目录解耦），确保 `AUTO_SEED=1` 时（含 Render）能正常 seed。测试仍 `AUTO_SEED=0` 不受影响。

### 验证（全部实跑通过）
- pytest **17/17** 通过（`test_applications.py` 4 例：CRUD/隔离/缺简历404/非法状态422；`test_upload.py` 2 例：txt/md解析/非法扩展名与空文件400）。
- ruff 全过；前端 `vite build` 通过（仅 1.1MB Element Plus chunk 警告，非阻塞）。
- **HTTP 端到端冒烟**（独立 `D:/` 库 + 后台 seed）：登录→seed 投递=5(全状态)→上传.txt/`.md`→建投递(201,快照正确)→改状态(applied)→非法状态422→过滤→删(204)→查删后404→第二账号越权404。全绿。
- 文档：`EXTENSIONS.md` 把 P0-1/2 标 ✅ 已实现（并订正库从 pdfplumber→pypdf）；`README.md` 已实现清单加两条。
- 工作树有未提交改动（本次新功能），**待用户用 GitHub Desktop 推送触发 Render 重部署上线**。本环境推 GitHub 偶发 TLS 中断。

## P0-3 分析多维化（四维子分 + 零依赖 SVG 雷达图）

用户"继续"→ 按 EXTENSIONS 排序做 P0-3：把 AI 分析的"一个数字"升级为技能/经验/学历/薪资四维诊断 + 雷达图。

### 后端
- `models.py` 的 `Analysis` 加 4 个可空 Float：`skill_match / exp_match / education_match / salary_fit`。
- `analysis.py`：`_SYSTEM_PROMPT` 要求 LLM 同时返回四维子分；新增 `_to_score()`（0-100 钳制、去"分"单位、None 容错）+ `_DIMENSION_KEYS`；`_parse_llm_output` 解析四维；`analyze` 落库四维。
- `schemas.py` 的 `AnalysisOut` 加 4 字段。
- `seed.py`：5 条示例分析补四维子分，seed 直接写入（无需 LLM key 即可看雷达）。
- **老库兼容（关键）**：`database.py` 的 `init_db()` 新增 `_ensure_analysis_dimension_columns()`——用 inspector 检查 analyses 表，缺列才 `ALTER TABLE ADD COLUMN`，幂等、SQLite/Postgres 通用。这样用户本地旧 `careerflow.db` 拉取后首次启动自动补齐，不会报缺列。另补 Alembic 迁移 `2a3b4c5d6e7f_add_analysis_dimensions.py`（down_revision=17ef322f6c5f）。
- 注意点：`create_all` 不会给已存在的旧表加列，这是加列必须处理的根因。

### 前端
- 新增 `frontend/src/components/RadarChart.vue`：手写 SVG 雷达（含网格/轴线/数据多边形/标签），**零依赖**（避免把 1.1MB chunk 进一步撑大）。
- `AnalysisView.vue`：引入雷达图，在「本次分析结果」卡片与「分析详情」弹窗展示四维雷达 + 分项进度条；历史表格新增「四维」彩色 chips 列。`types.ts` 的 `Analysis` 加 4 字段。

### 验证
- pytest **18/18**（新增 `test_parse_llm_output_dimensions` 验证钳制/去单位/null；`test_analysis_success` 断言四维落库）；ruff 全过（迁移文件 import 顺序已 `--fix`）。
- 前端 `vite build` 通过（AnalysisView 11.3kB）。
- **老库兼容单测通过**：手工建缺 4 列的旧 analyses 表 → `init_db()` 后 4 列自动补齐（missing=set()）。
- **HTTP 冒烟通过**：`/api/analysis` 返回 5 条 seed，每条均带 4 个 0-100 子分。
- 已提交（commit 见下条）。stray 测试库在 D:/（被 safe-delete 钩子保护未删，仓库外无碍）。

### 交付与下一步
- 提交 `233b55d` 之后，本轮单独提交（feat: P0-3 分析多维化 + 雷达图）。**推送仍交用户（GitHub Desktop）→ Render 重部署**。
- 路线建议下一步：P1 模拟面试对话（SSE 流式）或 P0-3 之外先做"分析缓存 UNIQUE(resume_id,job_id) 命中直接返回"（省 LLM 钱，技术债已记）。

## P0-A：薪资维度数据驱动 + 分析→面试交互闭环 + 示例数据完整化

用户质疑：截图里四维分数（尤其薪资 82）"凭什么给的"，交互乱、逻辑性差；要求示例数据能演示全部功能、逻辑严谨不僵硬；网站本人不用，只用来写入简历+用默认数据展示功能。

### 根因
- 旧 `analysis.py` 的 prompt 写的是"薪资区间与期望契合，**无明确信息时给合理估算**"，而示例简历/JD 里**根本没写薪资** → `salary_fit` 是 LLM 瞎估，无法解释。

### 改动（方案 A）
- **数据层**：`Resume` 加 `expected_salary`、`Job` 加 `salary_range`（String 255，可空默认""）；`schemas.py` 的 Create/Update/Out 全加；`resumes.py`/`jobs.py` CRUD 落字段；`database.py` 新增 `_ensure_salary_columns()` 老库 ALTER 兼容；新增 Alembic 迁移 `4c5d6e7f8091_add_salary_fields.py`(down_revision=3b4c5d6e7f80)。
- **Prompt 改造**：`analysis.py` 的 `_SYSTEM_PROMPT` 明确四维判定规则（技能0.35/经验0.30/学历0.20/薪资0.15 加权得综合分；薪资按期望与区间重叠度打分），并要求 `match_summary` **逐条点名四个维度**为什么给分；`analyze` 把 `expected_salary`/`salary_range` 拼进 user_prompt（未填则提示"按中性估算"）。
- **前端表单**：`ResumeView`/`JobView` 加期望薪资/薪资范围输入 + 查看弹窗展示；`types.ts`/`api.ts` 同步；`vue-tsc + vite build` 通过。
- **交互闭环**：`AnalysisView` 结果卡 + 详情弹窗都加「🎤 用本次分析开始模拟面试」按钮，带 `?analysis_id=` 跳 `/interview`；`InterviewView` 的 `onMounted` 读 query，命中则自动开启/复用该分析的面试（已存在则直接打开，避免重复）。详情弹窗展示"评分依据：期望薪资 vs 岗位薪资范围"。
- **示例数据完整化**：`seed.py` 4 份简历填 `expected_salary`、4 份 JD 填 `salary_range`（如实施 9k-13k × 8k-14k 高度重叠→salary_fit 80；实施×后端 9k-13k × 13k-20k 窄重叠→60）；5 条分析摘要全部点名薪资维度；**示例面试由"进行中1轮"升级为"已完成3轮"**（问→答→评价82→问→答→评价80→问→答→评价84，累计均分82），完整展示多轮评估+累计均分。

### 关键坑
- 第一次冒烟发现 `ResumeOut`/`JobOut` 漏加字段 → API 不返回薪资；补上后复测通过。
- seed 创建 `Resume`/`Job` 时**漏传** `expected_salary`/`salary_range`（dict 里有但构造没用）→ 实测返回空；补传后复测通过。
- 注意 `ASGITransport` 不触发 `lifespan` 事件 → 冒烟脚本需手动 `await _auto_seed()` 才能灌数据（真实部署由 main.py 启动事件触发，无此问题）。

### 验证（全绿）
- pytest **22/22**、ruff 全过；前端 `vue-tsc + vite build` 通过。
- HTTP 冒烟（临时库+手动 seed）：简历/ JD 均带薪资字段；分析#5(错配) `salary_fit=60` 且摘要含"薪资"；示例面试 `status=completed`、7 条消息、三轮评分 [82,80,84]、累计均分 82。
- 文档：`EXTENSIONS.md` 第 3 节补"薪资维度数据驱动"；`README.md` 第④步补薪资依据+一键面试说明。
- 提交（本地，待用户 GitHub Desktop 推送）：feat: 薪资维度数据驱动 + 分析→面试闭环 + 示例面试完整化。**推送仍交用户。**

## 收尾：全局检查 + 详尽项目总结 PROJECT_SUMMARY.md

用户"功能已完整，开始收尾，检查全局漏洞，确认无误后写详尽项目总结（供『二本计科生求职方向与规划咨询』项目提取信息）"。

### 全局检查结论：无功能性漏洞
- 后端 pytest **22/22**、ruff 零告警；前端 `vue-tsc + vite build` 通过（仅 1.1MB Element Plus chunk 警告，非阻断）。
- 一致性核对：路由前缀 `/api` 与前端 `api.ts` baseURL 一致；CORS 白名单；SPA catch-all 对 `api/` 返回 404 JSON；Schema/TS 类型对齐（AnalysisOut 含 match_summary/interview_questions/四维，ResumeOut/JobOut 含薪资字段）。
- 健壮性核对：`interview.py` 的 `_parse_turn` 对 LLM 非法 JSON 兜底（不 500，安全结束）；`next_question=null`→`completed`；`ask_llm` 失败→503；seed 幂等（空库才灌）。
- 部署核对：`config.py` 默认值合理（`auto_seed=True`、开发默认 SECRET_KEY 有告警）；`render.yaml` 配 `AUTO_SEED=1` + 随机 `SECRET_KEY` + dashscope/qwen-plus；Vite 代理 `/api→8000` 齐备。

### 交付物
- 新增 `PROJECT_SUMMARY.md`（约 290 行，自包含）：项目定位、技术栈表、架构目录、功能清单(对应真实场景)、**四维评分逻辑(薪资按区间重叠度,且强制点名依据)**、模拟面试交互、示例演示数据(账号 demo@careerflow.app/CareerFlow2026 + 4简历/4JD/5分析/5投递/1已完成三轮面试)、数据模型、API 速查、工程质量、部署、已知限制、二本计科生简历/面试话术要点。
- 提交 `1fc444b`（docs: PROJECT_SUMMARY.md）。**推送仍交用户（GitHub Desktop → Render 重部署）。**
- 已知限制(已在总结中记)：Render Free 临时盘重启丢库(设计取舍,auto_seed 恢复演示数据)；新注册非演示用户数据为空；前端包体未按需引入；分析无缓存 UNIQUE(resume_id,job_id)(技术债)。

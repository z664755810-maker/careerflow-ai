# 2026-08-27 工作记录

## 项目启动：CareerFlow AI（作品集 #3 / 启动包"项目 C"）

按 `启动包_第二项目.md` 要求，从零搭建现代全栈 AI 应用。主题按"默认用 1"选定 **CareerFlow AI（智能求职助手）**：上传简历/JD → LLM 分析匹配度 + 生成模拟面试问题。

### 关键工程决策
- **数据库双轨**：生产 PostgreSQL（asyncpg）+ Alembic 迁移；本地开发/测试用 SQLite（aiosqlite）兜底，保证零依赖可跑、CI 可绿、可移植性被验证。
- **LLM**：沿用启动包封装（OpenAI 兼容，key 仅走环境变量）。DeepSeek 余额不足，默认切**智谱 glm-4-flash（免费）**；网络探测 open.bigmodel.cn 可达(200)。调用失败优雅降级返回 503（已写测试）。
- **密码哈希**：passlib `pbkdf2_sha256`（避开 bcrypt 在 py3.13 的兼容坑），JWT 用 python-jose HS256。
- **前端**：Vue3 + Vite + TS + Pinia + Element Plus + vue-router，axios 自动带 JWT，路由鉴权守卫。
- **环境**：Docker 不可用 / psql 不可用（本机），故本地用 SQLite；Docker/compose/CI 配置按生产写法提供。

### 已完成并验证
- 后端全部接口（auth/resumes/jobs/analysis）用 FastAPI + SQLAlchemy 2.0 async 写好。
- **pytest 11 个用例全过**（含用户隔离、鉴权、LLM 降级）。
- **ruff lint 全过**。
- **uvicorn 冒烟测试**：/health 200、/docs 200、未鉴权 401、注册 201。
- **Alembic 初始迁移生成并应用**，表 users/resumes/jobs/analyses 在 SQLite 实跑建表成功。
- 前端 5 个页面 + store/router/api 封装写完；Docker 多阶段 + nginx 反代 + docker-compose 全栈；GitHub Actions CI（Postgres 服务容器跑 pytest+ruff + 前端构建）。
- git 初始化并首次提交 `2d5028c`。

### 进行中 / 待办
- 前端 `npm install` 正在后台跑，随后需 `npm run build` 验证 TS 编译与 Vite 构建。
- 部署上线（Render + Vercel 拿可点链接）需用户提供账号，按 README 步骤操作。
- 演示截图本地运行后补充。
- 智谱 LLM key 需用户注册获取（免费），填 .env 后 AI 分析才能真调通。

### 复跑命令速记
- 后端测试：`cd backend && <venv>/python -m pytest -q`
- 后端起服务：`cd backend && uvicorn app.main:app --reload --port 8000`（venv 在 `~/.workbuddy/binaries/python/envs/default`）

## P1-4 模拟面试对话（完成并验证）

按 EXTENSIONS 路线，把"静态面试题列表"升级为可对话的多轮模拟面试。

### 后端
- 新增 `InterviewSession` 模型（messages 用 JSON 文本存多轮对话、`current_score` 累计均分、`status` 状态机 in_progress→completed）。
- 路由 `api/interview.py`：`POST /interview`（开面试，首问取自分析的面试题）、`GET /interview`、`GET /{id}`、`POST /{id}/answer`（调 LLM 评价打分+反馈+下一题，累计均分；无 key 返回 503）、`DELETE /{id}`。全部按 owner_id 隔离，越权 404。
- Alembic 迁移 `3b4c5d6e7f80_add_interview_sessions.py` 建表。
- 测试 `tests/test_interview.py`：鉴权/开启/作答(mock)/隔离，全过。

### 前端
- 新增 `InterviewView.vue` 聊天式 UI：左栏历史会话、右栏气泡对话（面试官左/我右）、实时累计均分、结束锁输入、Ctrl+Enter 提交。
- `types.ts` 加 `InterviewSession/InterviewMessage`；`api.ts` 加 4 个函数；`router` 加 `/interview`；`App.vue` 加「🎤 模拟面试」导航。
- `npm run build`（含 vue-tsc）通过，InterviewView chunk 仅 7.76 kB。

### 踩坑（重要，已修）
- **seed 未 flush 导致示例面试会话丢失**：`seed.py` 建示例面试会话前查询 `Analysis`，但 analyses 只 `add` 未 `commit` 且会话 `autoflush=False`，查询拿到空 → `first_analysis` 为 None → 整段静默跳过。结果**部署到 Render 后模拟面试页是空的**（seed 日志还写死"1 面试"误导）。已加 `await db.flush()` 修复，端到端冒烟确认 seed 出 1 条含 3 消息、均分 82 的会话。
- 顺手清了 8 个历史 ruff 告警（test 文件未用变量/导入排序）。

### 验证
- `pytest 22 passed` + `ruff All checks passed!` + 前端构建通过 + HTTP 端到端冒烟（登录→开面试→作答→列表→详情→删除→越权隔离）全绿。
- 尚未提交（本地），待用户用 GitHub Desktop 推到 GitHub → Render 自动重部署。

# 2026-09-07 工作日志

## 邮箱验证码注册（路线 B：Mock 开发态，¥0）

用户问"登录注册加邮箱验证码要什么代价"，评估后选 **路线 B（开发态，零成本）**：完整两步注册（请求验证码→校验建号）+ 验证码表 + 60s 重发冷却 + 前端两步表单；**未配 SMTP 时验证码由接口直接返回前端展示，配了 SMTP 自动真发信（可插拔）**。

改动文件：
- `backend/app/models.py`：`VerificationCode` 模型（email/code/purpose/expires_at/used/created_at）
- `backend/app/config.py`：`SMTP_*` 配置 + `smtp_configured` 属性（HOST/USER/PASSWORD 齐备才视为已配）
- `backend/app/utils/email.py`（新建）：标准 SMTP 发信（兼容 Resend/Gmail/阿里云），开发态返回 code 不真发；失败抛异常由路由降级 503
- `backend/app/api/auth.py`：原 `POST /auth/register` 拆为 `/auth/register/request-code` + `/auth/register/verify`
- `backend/migrations/versions/5d6e7f8091ab_add_verification_codes.py`（新建迁移）
- 前端：`api.ts`/`stores/auth.ts`/`views/RegisterView.vue` 改为两步 + 倒计时 + 开发态验证码提示框
- 测试：`test_auth_verification.py`（开发态返回码/验证流程/错码400/已存在409/冷却429）；`conftest` 清表加 `verification_codes`
- 文档：PROJECT_SUMMARY.md（功能清单 #1、API 表、SMTP 环境变量、已知限制 #6）、README.md 同步

踩坑：① `Boolean` 导入漏改（Edit 未生效，重做）；② SQLite 读回 `DateTime(timezone=True)` 是 naive，与代码里 aware `now` 比较抛 `TypeError` → 统一改用 naive UTC（`datetime.utcnow()` + 读回值 `_naive()` 规整）。

验证：`pytest 27 passed` · `ruff` 全过 · 前端 `vue-tsc + vite build` 通过。

## 仍待办（用户此前已拍板，尚未做）
- 分析缓存去重：`UNIQUE(resume_id, job_id)` 命中即返回历史，省 LLM 调用费用（技术债，已记 EXTENSIONS.md）
- 前端按需引入 Element Plus，减小 `index.js`（约 1.1MB）首屏体积

## 提交
- 本地提交（推送仍交用户 GitHub Desktop → Render 重部署）。本次为 `feat: 邮箱验证码两步注册(开发态可插拔+冷却+测试)`。
